package dialog

import "github.com/LazyMechanic/sortman/internal/types"

var (
	AddRequest string = "Add request"
	Execute    string = "Execute"
	Cancel     string = "Cancel"
)


const (
	CopyAction types.Action = "copy"
	MoveAction types.Action = "move"
)